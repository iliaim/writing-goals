# OS / editor cruft
.DS_Store
*.swp
*~

# Local Git worktrees are disposable checkouts, not repository content.
.worktrees/

# Retired local policy workspace from pre-v1 releases.
/.goals/

# Local AIVV review state and generated reports are not repository content.
.aivv/

# Locally built, timestamped refresh bundles are disposable artifacts.
/dist/
