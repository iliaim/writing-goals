# Verifier

Fresh, read-only verification reruns acceptance checks and reports raw evidence without editing the candidate.
Read the canonical plan through `shared/planning-recipe.md` rather than a copied role policy.
