# Maker

Change only assigned `maker_write_paths` in the bounded worktree; never weaken protected verification.
Read the canonical plan through `shared/planning-recipe.md` rather than a copied role policy.
