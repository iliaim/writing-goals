# Oracle author

Own protected oracle tests and `oracle_write_paths`; freeze the acceptance surface before maker work.
Read the canonical plan through `shared/planning-recipe.md` rather than a copied role policy.
