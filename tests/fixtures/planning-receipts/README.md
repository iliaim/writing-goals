# G14 capsule and receipt fixtures

`valid` contains the single canonical plan and its selected immutable manifest.
The other directories model prohibited persistent duplicate authority, copied role
policy, automated semantic approval, and generated dispatch state. The receipt
renderer validates a fixture directory only when called with `--validate-fixture`.
